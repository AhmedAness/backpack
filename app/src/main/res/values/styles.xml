<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:typeface">serif</item>
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:background">@color/white</item>
        <item name="colorControlNormal">@color/bg_text_color</item>
        <item name="android:textColor">@color/bg_text_color</item>
    </style>

    <style name="AppTheme.AppBarOverlay2" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@color/bg_text_color</item>
        <item name="android:textColor">@color/bg_text_color</item>
        <item name="android:background">@color/white</item>

    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@color/white</item>
        <item name="colorControlNormal">@color/bg_text_color</item>
        <item name="android:textColor">@color/bg_text_color</item>
    </style>

    <style name="AppTheme.PopupOverlay3" parent="ThemeOverlay.AppCompat.Light">

        <item name="android:background">@color/zxing_transparent</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="android:textColor">@color/white</item>
        //toolbar back arrow color
        <item name="android:textColorSecondary">@android:color/white</item>
        //toolbar title color
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <style name="AppTheme.PopupOverlay_red" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="YourActivityTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="actionMenuTextColor">@android:color/black</item>
        <item name="android:textColor">@android:color/black</item>


    </style>

    <style name="dialog_theme" parent="Theme.AppCompat.Light">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>


        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="actionMenuTextColor">@android:color/black</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:windowEnterAnimation">@anim/slide_out_up</item>
        <item name="android:windowExitAnimation">@anim/slide_in_up</item>

    </style>

    <style name="MyAppTheme" parent="@android:style/Theme.Holo.Light">
        <!-- you should set typeface which you want to override with TypefaceUtil -->
        <item name="android:typeface">serif</item>
    </style>
</resources>
